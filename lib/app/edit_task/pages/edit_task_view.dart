import 'package:flutter/material.dart';

class EditTaskView extends StatelessWidget {
  const EditTaskView({super.key});
  static const String id = 'EditTaskView';

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
